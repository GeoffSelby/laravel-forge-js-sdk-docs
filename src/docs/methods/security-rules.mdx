---
title: 'Security Rules'
description: Laravel Forge JavaScript SDK security rules module.
---

## Create a security rule

### Method

`forge.security.create(server_id, site_id, payload)`

### Usage

```js lineNumbers=true
const forge = new Forge('API_TOKEN');

const securityRule = await forge.security.create(server_id, site_id, payload);
```

### Payload

```js lineNumbers=true
{
  name: 'Access Restricted',
  path: null,
  credentials: [
    {
      username: 'taylor.otwell',
      password: 'password123'
    },
    {
      username: 'james.brooks',
      password: 'secret123'
    }
  ]
}
```

### Example Response

```json lineNumbers=true
{
  "security_rule": {
    "id": 15,
    "name": "Access Restricted",
    "path": null,
    "created_at": "2020-07-30 10:11:10",
    "credentials": [
      {
        "id": 20,
        "username": "taylor.otwell",
        "created_at": "2020-07-30 10:11:10"
      },
      {
        "id": 21,
        "username": "james.brooks",
        "created_at": "2020-07-30 10:11:10"
      }
    ]
  }
}
```

---

## List all security rules

### Method

`forge.security.list(server_id, site_id)`

### Usage

```js lineNumbers=true
const forge = new Forge('API_TOKEN');

const securityRules = await forge.security.list(server_id, site_id);
```

### Example Response

```json lineNumbers=true
{
  "security_rules": [
    {
      "id": 15,
      "name": "Access Restricted",
      "path": null,
      "created_at": "2020-07-30 10:11:10",
      "credentials": [
        {
          "id": 20,
          "username": "taylor.otwell",
          "created_at": "2020-07-30 10:11:10"
        },
        {
          "id": 21,
          "username": "james.brooks",
          "created_at": "2020-07-30 10:11:10"
        }
      ]
    }
  ]
}
```

---

## Get a security rule

### Method

`forge.security.get(server_id, site_id, rule_id)`

### Usage

```js lineNumbers=true
const forge = new Forge('API_TOKEN');

const securityRule = await forge.security.get(server_id, site_id, rule_id);
```

### Example Response

```json lineNumbers=true
{
  "security_rule": {
    "id": 15,
    "name": "Access Restricted",
    "path": null,
    "created_at": "2020-07-30 10:11:10",
    "credentials": [
      {
        "id": 20,
        "username": "taylor.otwell",
        "created_at": "2020-07-30 10:11:10"
      },
      {
        "id": 21,
        "username": "james.brooks",
        "created_at": "2020-07-30 10:11:10"
      }
    ]
  }
}
```

---

## Delete a security rule

### Method

`forge.security.delete(server_id, site_id, rule_id)`

### Usage

```js lineNumbers=true
const forge = new Forge('API_TOKEN');

await forge.security.delete(server_id, site_id, rule_id);
```

---
